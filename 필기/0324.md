#0324 수업

```
./폴더/파일 : 전폴더로

../ : 전전폴더로

내가 폴더 안에 있으면 굳이 경로를 쓸필요x

나가서 다시 들어오기 ../../
```

```
제이쿼리 download google cdn

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script> head에 붙여넣기




```

```
css가 안될때는 jquery로 해야함
<script>는 왠만하면 마지막에
```

```
시용방법
<div>

<script>
$("div").css("background-color","red");
$("div").children().css("background-color","blue");
$("div").children().append("자식이 하나 더") ;
$("div").children().prepend("자식이 하나 더") ;

append:추가로 붙이기
prepend:앞에 붙이기

<style>
width : 100px

한번에 주석처리 ctrl+/

jquery >> API Documentation 들어가면 여러개 있음
```